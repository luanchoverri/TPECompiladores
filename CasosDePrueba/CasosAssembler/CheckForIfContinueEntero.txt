rogram
{
   i32 a,b;
       a =: 3;
       b =: a * 2 + 5;

       for (i =: 0; i < 6; +i) {

           if (a < 3) then{
               a =: b;
               out('a era menor');
           } else {

             out('entre a 0');
               a =: 0;
               continue;
           } end_if;
       };
}
