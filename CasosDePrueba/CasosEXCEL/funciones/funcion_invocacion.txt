program{

    fun g(i32 i):f32{
        out('entre en funcion');
        if (i = 1) then {
            out('entre al if');
            return (2.0);
        } end_if;
    }

    i32 a;
    f32 b;

    a =: 1;
    b =: g(1);

    if (b = 2.0) then {
        out('se retorno correctamente');
    } end_if;

}